collection server sẽ gồm
id
name string
ownerId: objectId
iconURL string
memberId[] dùng để lưu id của người tham gia server
channelId[] dùng để lưu channel trong server
CatalogId[] dùng để lưu catalog trong server
createdAt
updateAt


collection catalog sẽ gồm 
id
title
channelId []
createdAt
updateAt

collection textChannel sẽ gồm
id
title
createdAt
updateAt

collection voiceChannel
id
title
createdAt
updateAt


messageP2P sẽ gồm
id
senderId
receiverId
message: string
imageMessage: string (sẽ chứa url ảnh được lưu trong cloud)
createdAt
updateAt

messageGroup sẽ gồm
id:
senderId:
channelId:
message: string
imageMessage: string (sẽ chứa url ảnh được lưu trong cloud)
createdAt
updateAt

friendship sẽ gồm
id
senderId
receiverId
status: (giá trị default sẽ là pending)
createdAt
updateAt

vì có thêm collection friendship, nên tôi nhờ bạn sửa lại interface của user sẽ có thêm trường friend:[userId], và nhờ bạn kiểm tra lại module auth register, giá trị default sẽ là mảng rỗng ở field friend 


async deleteFriendship(id: string): Promise<IFriendshipDocument> {
    const friendship = await FriendshipModel.findById(id);

    if (!friendship) {
      throw new ApiError(404, 'Friendship not found');
    }

    if (friendship.status === FriendshipStatus.ACCEPTED) {
      await Promise.all([
        User.updateOne({ _id: friendship.senderId }, { $pull: { friends: friendship.receiverId } }),
        User.updateOne({ _id: friendship.receiverId }, { $pull: { friends: friendship.senderId } }),
      ]);
    }

    await FriendshipModel.findByIdAndDelete(id);

    return friendship;
  },
};

vậy khi delete friendship thì có xóa id của 2 người ra khỏi field friend không ?





chức năng login đã hoạt động đúng, giờ hãy viết giao diện trang chủ của discord sau khi login thành công 

<div class="catalog-title">Main Channels</div>, tại những catalog như này, hãy thêm cho tôi icon dấu + và mũi tên xuống, icon dấu + dùng để thêm channel, icon mũi tên xuống dùng để rút gọn channel trong catalog đó

khi bấm vào icon dấu +, đó là chức năng tạo mới channel, khi ấn vào hãy hiển thị bảng để người dùng lựa chọn thêm channel voice/ channel text , và ô input để nhập title cho channel

tại button này <button class="server-item server-item--add" title="Thêm máy chủ"><span class="server-icon">+</span></button>, đây là tạo mới server , khi ấn vào hãy hiển thị bảng để người dùng nhập thông tin để tạo server, xem E:\Discord_remake\BE\src\types\server\request.ts  để rõ hơn cấu trúc của  server 

khi ấn chuột phải vào channel, hãy thêm cho tôi 2 sự lựa chọn là sửa hoặc xóa channel

<input type="text" class="modal-input" placeholder="https://example.com/icon.png" value=""> tại nút này (tạo server) hãy cho người dùng lấy ảnh từ máy tính của họ, và có thể xem trước ảnh của server trước khi tạo


<span class="server-icon">🏠</span> khi người dùng nhấn vào icon này hãy cho chuyển sang trang để trò chuyện với bạn bè, và phía trên sẽ có cách nút như
"Trực tuyến"
"Tất cả"
"Đang chờ xử lý"
"Thêm bạn"

sau khi login tôi thấy giao diện chỉ có màu xanh, kiểm tra lại phần chuyển trang khi login thành công

tôi muốn trang <span class="server-icon">🏠</span> và trang <span class="server-icon">M</span> dùng chung cái thanh bên trái này nè <nav class="server-rail"><button class="server-item server-item--home server-item--active" title="Trang chủ"><span class="server-icon">🏠</span></button><div class="server-divider"></div><button class="server-item " title="Main Server"><span class="server-icon">M</span></button><button class="server-item " title="Fun Server"><span class="server-icon">F</span></button><button class="server-item server-item--add" title="Thêm máy chủ"><span class="server-icon">+</span></button></nav>


<nav class="server-rail"><button class="server-item server-item--home " title="Trang chủ"><span class="server-icon">🏠</span></button><div class="server-divider"></div><button class="server-item server-item--active" title="Main Server"><span class="server-icon">M</span></button><button class="server-item " title="Fun Server"><span class="server-icon">F</span></button><button class="server-item server-item--add" title="Thêm máy chủ"><span class="server-icon">+</span></button></nav>
thanh này rộng hơn 1 xíu nữa, và khi nhấn vào server hoặc main  thì hiện một dấu trắng bên trái icon để người dùng biết mình đang ở đâu



tại trang này <span class="server-icon">🏠</span>, bạn hãy thêm cho tôi nút search để kết bạn, E:\Discord_remake\BE\API.md đọc file này để biết API


<div class="sidebar-scroll"><div class="catalog-group"><div class="catalog-title"><span class="catalog-arrow " aria-hidden="true">▼</span><span class="catalog-title-text">Main Channels</span><button class="catalog-add" title="Thêm kênh">+</button></div><button class="channel-item channel-item--active"><span class="channel-icon">#</span><span class="channel-name">general</span></button><button class="channel-item "><span class="channel-icon">🔊</span><span class="channel-name">ád</span></button></div><div class="catalog-group"><div class="catalog-title"><span class="catalog-arrow " aria-hidden="true">▼</span><span class="catalog-title-text">Fun Channels</span><button class="catalog-add" title="Thêm kênh">+</button></div><button class="channel-item "><span class="channel-icon">#</span><span class="channel-name">random</span></button></div></div>

tôi muốn bạn thêm thanh này ở FriendsPage và nó sẽ chứa icon của bạn bè đã kết bạn, hãy làm cho nó giống với giao diện của serverPage


E:\Discord_remake\BE\src\config\cloudinary.ts đọc qua file này tôi vừa thêm vào, và đọc thêm import { InternalServerError } from '../errors/AppError.js';
import { UploadErrorCode } from '../types/upload/enums.js'; để hiểu cấu trúc folder hiện tại

E:\Discord_remake\BE\src\middlewares\uploadMiddleware.ts tại file này tại sao dòng import multer lại bị lỗi type và sửa lỗi đó cho tôi

export const uploadVideo = async (
    videoBuffer: Buffer,
    folder: string = 'videos/lectures'

    dòng folder kia là lưu ở máy tính của mình hay lưu ở cloud vậy 